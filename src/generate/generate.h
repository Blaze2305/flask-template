#ifndef GENERATE_H_

#define GENERATE_H_

#include <string>
#include <filesystem>
#include <iostream>
#include <map>
#include <sstream>
#include <fstream>

void generateFolders(std::string projName);

void generateFiles(std::string projName);

#endif